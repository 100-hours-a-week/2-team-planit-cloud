# AI API Service
apiVersion: v1
kind: Service
metadata:
  name: planit-ai-service
  namespace: planit-app
spec:
  selector:
    app: planit-ai
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
